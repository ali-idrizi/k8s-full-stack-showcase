containerPorts:
  http: 3001
extraEnvFrom:
  - configMapRef:
      name: nats
image:
  repository: user
ingress:
  path: /api/user(/|$)(.*)
  annotations:
    nginx.ingress.kubernetes.io/server-snippet: |
      location ~ ^/api/user/auth/?$ {
        internal;
      }
