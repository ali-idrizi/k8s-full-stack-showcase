auth:
  extraEnvVars:
    - name: JWT_SECRET
      value: Nsg0ZhZjJVPvrYrYiJWmcIOn

  db:
    enabled: true
    host: db-auth.dev.svc.cluster.local
    username: auth
    password: password
    database: auth

todo:
  db:
    enabled: true
    host: db-todo.dev.svc.cluster.local
    username: todo
    password: password
    database: todo

user:
  db:
    enabled: true
    host: db-user.dev.svc.cluster.local
    username: user
    password: password
    database: user
