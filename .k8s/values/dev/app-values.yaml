ingress: &ingress
  auth:
    url: http://dev-app-user.dev.svc.cluster.local/auth

auth:
  db:
    prisma:
      migrate:
        enabled: false

todo:
  ingress:
    <<: *ingress
  db:
    prisma:
      migrate:
        enabled: false

user:
  ingress:
    <<: *ingress
  db:
    prisma:
      migrate:
        enabled: false
      seed:
        enabled: false

webClient:
  ingress:
    <<: *ingress

nats:
  host: nats.dev.svc.cluster.local
